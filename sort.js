// 快速排序
  function quickSort(arr) {
    // 从中间分
    if (arr && arr.length){
      const middle = Math.floor(arr.length/2)
      for () {
        
      }
    } else {
      return true
    }
  }
//归并排序