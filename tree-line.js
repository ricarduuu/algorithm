/* 平面中有n个元素，每个元素有4个顶点，四个中心点
* 拖动其中一个元素，算法即启动对比，当前元素的位置和其他元素的关联
* 如果靠近，就显示出线条，线条位于拖动的元素体上
*/

// 　首先需要一个生成器，生成 规则的长方形坐标，并限定在一定的范围内，比如说 手机的宽高 / pc的宽高

// 生成30个 element

// 生成一个element， 模拟其拖动，进行坐标的对比

// 真实环境还会 涉及到 对比确认后的浏览器渲染耗时，某一时间段内的多次命中，
